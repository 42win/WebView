<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devtekid.wafi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".TentangActivity" />
        <activity android:name=".SampahBermanfaatActivity" />
        <activity android:name=".SampahBerhargaActivity" />
        <activity android:name=".HargaPlastikActivity" />
        <activity android:name=".HargaLogamActivity" />
        <activity android:name=".HargaKacaActivity" />
        <activity android:name=".HargaKertasActivity" />
        <activity android:name=".HargaLainActivity"></activity>
    </application>

</manifest>